interface=wlan0
driver=nl80211
ssid=DidiCafe
hw_mode=a
channel=36
ieee80211n=1
ieee80211ac=1
wmm_enabled=1

# Open network -- captive portal handles authentication
auth_algs=1
wpa=0

# Client limit
max_num_sta=50

# Country code (Madagascar)
country_code=MG
ieee80211d=1
